server {
    # Docker embedded DNS; re-resolve containers after recreation.
    resolver 127.0.0.11 valid=10s ipv6=off;
    set $php_backend app:9000;

    listen 80;
    server_name _;
    root /var/www/html/public;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    index index.php;
    charset utf-8;
    client_max_body_size 25m;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico {
        access_log off;
        log_not_found off;
    }

    location = /robots.txt {
        access_log off;
        log_not_found off;
    }

    error_page 404 /index.php;

    location ~ \.php$ {
        fastcgi_pass $php_backend;

        # nginx's compile-time default (4-8k) is too small for a Laravel
        # response with several large cookies (session + XSRF-TOKEN, both
        # bigger under SESSION_ENCRYPT=true) — PHP-FPM's headers alone can
        # exceed it, which nginx reports as a 502 "upstream sent too big
        # header" with no clue in the app's own logs (see docs/DEPLOYMENT.md).
        fastcgi_buffer_size 16k;
        fastcgi_buffers 4 16k;

        include fastcgi_params;

        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;

        # Caddy terminates TLS before forwarding to this container.
        fastcgi_param HTTPS on;
        fastcgi_param REQUEST_SCHEME https;
        fastcgi_param SERVER_PORT 443;
        fastcgi_param HTTP_X_FORWARDED_PROTO https;
        fastcgi_param HTTP_X_FORWARDED_PORT 443;
        fastcgi_param HTTP_X_FORWARDED_HOST $host;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
